# Only FastAPI is pinned to an exact release. Everything below is a floor, so pip resolves
# a compatible set rather than failing on a version that does not exist.
#
# This file previously pinned starlette==0.52.1, which has never been published — the newest
# release is 0.49.x — so a clean install failed outright and the server could not start.
# The other exact pins were written the same way and could not be verified, so they are
# floors now. Regenerate real pins with `.venv/bin/python -m pip freeze` from an install
# that actually worked, rather than writing version numbers by hand.
fastapi==0.128.8

# Never pin starlette here: FastAPI declares the range it works with, and a hand-written pin
# will contradict it. Let pip resolve it.

uvicorn[standard]>=0.30
httpx>=0.28

# Pure-Python RS256 verification works on both Intel Macs and production Linux.
rsa>=4.9

PyNaCl>=1.5
email-validator>=2.2
eval-type-backport>=0.2.2

# Transitive helpers kept at or above their patched releases.
click>=8.1.7
python-dotenv>=1.0.1
