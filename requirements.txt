# Tested on Python 3.12. Update these together and rerun API and dependency audits.
fastapi==0.141.1
starlette==1.6.0

uvicorn[standard]>=0.30
httpx>=0.28

# Pure-Python RS256 verification works on both Intel Macs and production Linux.
rsa>=4.9

PyNaCl>=1.5
email-validator>=2.3.0
eval-type-backport>=0.2.2

# Transitive helpers kept at or above their patched releases.
click>=8.1.7
python-dotenv>=1.0.1
